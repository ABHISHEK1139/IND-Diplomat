# Deploy

Deployment and bootstrap consolidation lives here during the repo migration.

- Root container files stay in place for backward compatibility.
- `system_bootstrap/` is still available and is also exposed through `ind_diplomat.bootstrap`.
